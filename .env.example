# TI-Monitoring Notification Settings Password
# Copy this file to .env and set a secure password
NOTIFICATION_SETTINGS_PASSWORD=your_secure_password_here

# SSL Configuration
# Set your domain name and email for Let's Encrypt SSL certificate
SSL_DOMAIN=ti-monitoring.example.com
SSL_EMAIL=admin@example.com

# PostgreSQL / TimescaleDB (optional, bei Nutzung des Compose-Profils "tsdb")
# Hinweis: Werte werden von docker-compose in DB und App (cron) injiziert
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=timonitor
POSTGRES_USER=timonitor
POSTGRES_PASSWORD=timonitor